import { describe,expect,it } from "vitest";
import { LocalRequirementExtractor, paragraphs } from "@/lib/extraction";
import { LocalEvidenceMatcher, expiryState } from "@/lib/matching";

describe("lokale extractie",()=>{it("extraheert eisen uit TXT-achtige tekst",()=>{const out=new LocalRequirementExtractor().extract(paragraphs("De leverancier moet alle gegevens versleutelen.\n\nVrije inleiding zonder norm."));expect(out).toHaveLength(1);expect(out[0].category).toBe("informatiebeveiliging");expect(out[0].fragment).toContain("versleutelen")})});
describe("lokale matcher",()=>{it("maakt een traceerbaar voorstel",()=>{const requirement={id:"r",projectId:"p",sourceDocumentId:null,number:"E-001",title:"Encryptie",originalText:"Gegevens moeten versleuteld zijn",category:"informatiebeveiliging",sourceLocation:"Alinea 1",sourceFragment:"Gegevens moeten versleuteld zijn",priority:"verplicht",origin:"automatisch",status:"niet beoordeeld",notApplicable:false,createdAt:new Date()};const evidence=[{id:"e",projectId:"p",title:"Encryptiebeleid",documentType:"beleidsdocument",description:"Beleid voor encryptie",organization:"Planwijzer Systemen B.V.",product:"Planwijzer AI",productVersion:"2.3",environment:"productie",owner:"Nora",issuedAt:null,expiresAt:null,confidentiality:"intern",tags:"encryptie beveiliging",fileName:"beleid.txt",storedName:"x.txt",mimeType:"text/plain",extractedText:"Gegevens zijn in opslag versleuteld met AES-256.",uploadedAt:new Date()}];const project={id:"p",name:"x",client:"x",reference:"x",dueDate:null,product:"Planwijzer AI",productVersion:"2.3",owner:"Eva",notes:"",createdAt:new Date()};const out=new LocalEvidenceMatcher().match(requirement,evidence,project);expect(out[0].score).toBeGreaterThan(10);expect(out[0].fragment).toContain("AES-256")})});
describe("geldigheid",()=>{it("herkent verlopen bewijs",()=>expect(expiryState(new Date("2020-01-01"),new Date("2026-01-01"))).toBe("verlopen"));it("herkent bewijs dat binnen 30 dagen verloopt",()=>expect(expiryState(new Date("2026-01-20"),new Date("2026-01-01"))).toBe("verloopt binnenkort"))});

